services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: pagination_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - '5434:5432'
    tmpfs:
      - /var/lib/postgresql/data
